import React from 'react'
import { StyleSheet, View } from 'react-native'

const SignUp = (props) => {
    return <View style={StyleSheet.root}></View>
}

const styles = StyleSheet.create({
    root: {
        padding: 15,
    },
})

export default SignUp
